name: CMake pack sources

on:
  push:
    branches: [ "master" ]

env:
  # Customize the CMake build type here (Release, Debug, RelWithDebInfo, etc.)
  BUILD_TYPE: Release

jobs:
  build:
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set reusable strings
      # Turn repeated input strings (such as the build output directory) into step outputs. These step outputs can be used throughout the workflow file.
      id: strings
      shell: bash
      run: |
        echo "build-output-dir=${{ github.workspace }}/build" >> "$GITHUB_OUTPUT"

    - name: Configure CMake
      # Configure CMake in a 'build' subdirectory. `CMAKE_BUILD_TYPE` is only required if you are using a single-configuration generator such as make.
      # See https://cmake.org/cmake/help/latest/variable/CMAKE_BUILD_TYPE.html?highlight=cmake_build_type
      run: >
        cmake -B ${{ steps.strings.outputs.build-output-dir }}
        -DCMAKE_BUILD_TYPE=${{ matrix.build_type }}

    - name: Build GLTemplate.zip
      run: cmake --build ${{ steps.strings.outputs.build-output-dir }} --target GLTemplate-zip --config ${{ matrix.build_type }}

    - uses: actions/upload-artifact@v4
      with:
        name: complete
        path: ${{ steps.strings.outputs.build-output-dir }}/GLTemplate.zip

    - name: Build GLTemplate.zip
      run: cmake --build ${{ steps.strings.outputs.build-output-dir }} --target GLTemplate-minimal-zip --config ${{ matrix.build_type }}

    - uses: actions/upload-artifact@v4
      with:
        name: minimal
        path: ${{ steps.strings.outputs.build-output-dir }}/GLTemplate-minimal.zip
